# webfinal
## Authentication & Security

This part of the project is responsible for user registration, login, JWT-based authentication, role-based access control (RBAC), validation and error handling.

### Environment variables

The following variables are required for authentication:

```env
JWT_SECRET=yourSuperSecretKeyHere
